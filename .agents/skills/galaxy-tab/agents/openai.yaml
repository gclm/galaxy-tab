interface:
  display_name: "Galaxy Tab"
  short_description: "Repo-specific patterns and workflows for galaxy-tab"
  default_prompt: "Use the galaxy-tab repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true